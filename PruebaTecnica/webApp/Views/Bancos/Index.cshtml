@model List<webApp.Models.Banco>
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <br />
    <br />
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Categoria</th>
            </tr>
        </thead>
        <tbody>


            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Banco1</td>
                </tr>
            }
        </tbody>
    </table>
</div>